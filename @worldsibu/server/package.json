{
  "name": "@worldsibu/convector-example-dsc-server",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/src/app.js",
  "private": true,
  "scripts": {
    "start": "npm run build && pm2-dev pm2.config.json",
    "start:daemon": "pm2 startOrRestart pm2.config.json",
    "stop": "pm2 stop pm2.config.json",
    "clean": "rimraf dist client",
    "build": "npm run clean && lerna bootstrap && tsc && cp -R ./src/config ./dist/"
  },
  "author": "WorldSibu",
  "license": "Apache-2.0",
  "dependencies": {
    "@worldsibu/convector-adapter-fabric": "1.1.5",
    "@worldsibu/convector-storage-couchdb": "1.1.5",
    "@worldsibu/convector-example-dsc-cc-drug": "^1.0.0",
    "body-parser": "^1.18.3",
    "config": "^1.30.0",
    "dotenv": "^6.0.0",
    "express": "^4.16.3",
    "node-couchdb": "^1.3.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "pm2": "^3.0.0",
    "rimraf": "^2.6.2",
    "ts-node": "^7.0.0",
    "typescript": "^2.9.2"
  }
}
